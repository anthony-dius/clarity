# Status Report

The team finished the migration on Tuesday. We verified every record.

Run the script to check results. The script prints a summary line for each file.

The build takes four minutes. Tests run in parallel on two workers.
