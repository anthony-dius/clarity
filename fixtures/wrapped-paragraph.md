# Wrapped Paragraph Fixture

The team reviewed the proposal and agreed it addressed the concern before
this deadline came due.

Here is a sentence that intentionally spans three wrapped lines so that its
total word count crosses the long-sentence threshold once every line is
joined back together into the single sentence the author actually wrote.

- First list item stays on its own line.
- Second list item also stays on its own line.

## A Heading

```text
const example = "code fences are excluded from checking";
```

| Column A | Column B |
| --- | --- |
| value one | value two |
